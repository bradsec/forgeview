---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - vite.config.ts
  - tsconfig.json
  - src/styles/index.css
  - src/main.tsx
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running pnpm tauri dev opens a native window displaying a React app"
    - "All pinned dependency versions are installed exactly as specified"
    - "Tailwind v4 utility classes render correctly in the browser"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all pinned dependencies"
      contains: "three"
    - path: "vite.config.ts"
      provides: "Vite config with React and Tailwind v4 plugins"
      contains: "tailwindcss"
    - path: "src/styles/index.css"
      provides: "CSS entry with Tailwind v4 import"
      contains: "@import"
    - path: "src-tauri/Cargo.toml"
      provides: "Rust dependencies including tauri plugins"
      contains: "tauri-plugin-dialog"
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri window config (1280x800, 800x600 min)"
      contains: "Forge View"
  key_links:
    - from: "vite.config.ts"
      to: "src/styles/index.css"
      via: "Tailwind vite plugin processes CSS"
      pattern: "tailwindcss"
    - from: "src/main.tsx"
      to: "src/styles/index.css"
      via: "CSS import in entry point"
      pattern: "import.*styles"
---

<objective>
Bootstrap the forge-view Tauri v2 + React 19 + TypeScript project from scratch, install all dependencies at pinned versions, and configure Vite + Tailwind v4.

Purpose: Establish the project scaffold that all subsequent plans build upon.
Output: A working Tauri dev environment that opens a native window with a React app inside.
</objective>

<execution_context>
@/home/mark/.claude/get-shit-done/workflows/execute-plan.md
@/home/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri project and install all dependencies</name>
  <files>package.json, pnpm-lock.yaml, src-tauri/Cargo.toml, src-tauri/tauri.conf.json</files>
  <action>
1. Bootstrap the project using the Tauri scaffolder. Since we are already in the forge-view directory, scaffold into a temporary directory and move files, OR scaffold in the parent directory and merge. The key constraint: the final project root must be /home/mark/Code/forge-view with the standard Tauri + React-TS template structure.

   Run: `pnpm create tauri-app@latest forge-view-tmp --template react-ts --manager pnpm` from the parent directory, then move all generated files into /home/mark/Code/forge-view (preserving .planning/ and CLAUDE.md).

   Alternative if the directory already has content that conflicts: scaffold into /tmp, then copy the template files over.

2. Install frontend dependencies at exact pinned versions:
   ```
   pnpm add three@0.183.2 zustand@5.0.11
   pnpm add -D @types/three@0.183.1 tailwindcss@4.2.1 @tailwindcss/vite@4.2.1
   pnpm add @tauri-apps/plugin-dialog@2 @tauri-apps/plugin-fs@2
   ```

3. Add Rust plugin dependencies to src-tauri/Cargo.toml:
   ```toml
   tauri-plugin-dialog = "2"
   tauri-plugin-fs = "2"
   serde = { version = "1", features = ["derive"] }
   serde_json = "1"
   ```
   Note: tokio is already included by Tauri. Do NOT add tokio separately unless the template omits it.

4. Update src-tauri/tauri.conf.json to match CLAUDE.md spec:
   - Window title: "Forge View"
   - Window size: 1280x800
   - Min size: 800x600
   - Bundle identifier: "com.forgeview.app"
   - Add plugins section for dialog and fs with scope
   - Add security.capabilities referencing "default"

CRITICAL: Preserve all existing files in the repo (.planning/, CLAUDE.md, .git/ if present).
  </action>
  <verify>
    <automated>cd /home/mark/Code/forge-view && test -f package.json && test -f src-tauri/Cargo.toml && test -f src-tauri/tauri.conf.json && node -e "const p=require('./package.json'); const ok = p.dependencies?.three === '0.183.2' && p.dependencies?.zustand === '5.0.11'; if(!ok) { console.error('Version mismatch'); process.exit(1); } console.log('Versions OK')"</automated>
  </verify>
  <done>package.json has all dependencies at pinned versions. Cargo.toml has tauri-plugin-dialog and tauri-plugin-fs. tauri.conf.json has correct window config.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vite, Tailwind v4, and CSS entry point</name>
  <files>vite.config.ts, src/styles/index.css, src/main.tsx</files>
  <action>
1. Update vite.config.ts to include both React and Tailwind v4 plugins:
   ```typescript
   import { defineConfig } from 'vite'
   import react from '@vitejs/plugin-react'
   import tailwindcss from '@tailwindcss/vite'

   export default defineConfig({
     plugins: [react(), tailwindcss()],
   })
   ```
   IMPORTANT: Do NOT add a Tauri-specific host/port config — the template may already have one. Preserve any existing Tauri-related config (like `clearScreen: false`, `server.strictPort`).

2. Create or update src/styles/index.css with Tailwind v4 CSS-first import:
   ```css
   @import "tailwindcss";
   ```
   NO @tailwind directives (that's v3 syntax). NO tailwind.config.js file. NO postcss.config.js.

3. Update src/main.tsx to import the CSS file. The template likely imports a CSS file already — ensure it points to `./styles/index.css`. Ensure it uses `createRoot` (React 19 — NOT `ReactDOM.render`).

4. Verify the Tailwind setup works by ensuring the template's default component uses at least one Tailwind class. If not, add a simple one (e.g., `className="bg-gray-900 text-white"` on the root div).

5. Delete any tailwind.config.js or postcss.config.js if the template created them — they are NOT used in Tailwind v4.
  </action>
  <verify>
    <automated>cd /home/mark/Code/forge-view && grep -q '@tailwindcss/vite' vite.config.ts && grep -q '@import "tailwindcss"' src/styles/index.css && echo "Tailwind v4 configured"</automated>
  </verify>
  <done>vite.config.ts has both react() and tailwindcss() plugins. src/styles/index.css uses @import "tailwindcss". No tailwind.config.js exists. The app compiles with pnpm build (frontend only).</done>
</task>

</tasks>

<verification>
Run `pnpm install` and `pnpm build` (frontend-only Vite build) to confirm TypeScript compiles and Tailwind processes correctly. Then run `pnpm tauri dev` to confirm the native window opens with the React app. The Rust backend may show warnings about unused plugins — that is expected and will be resolved in Plan 02.
</verification>

<success_criteria>
1. `pnpm tauri dev` opens a native Tauri window displaying the React template app
2. All dependency versions in package.json match pinned versions exactly
3. Tailwind v4 classes render (no tailwind.config.js exists, CSS uses @import)
4. No TypeScript compilation errors from `pnpm build`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
